<dialogs:CustomDialog x:Class="POS.MainPOS.Views.CustomDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:dialogs="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             xmlns:views="clr-namespace:POS.MainPOS.Views"
            xmlns:vm="clr-namespace:POS.MainPOS.ViewModels"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance vm:CustomDialogViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <dialogs:CustomDialog.Content>
        <ScrollViewer>
            <ContentControl>
                <views:DialogContentView DataContext="{Binding DialogContectViewModel}"/>
            </ContentControl>
        </ScrollViewer>
    </dialogs:CustomDialog.Content>
</dialogs:CustomDialog>
