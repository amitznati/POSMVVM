<UserControl x:Name="userControl" x:Class="POS.MainPOS.Views.DialogContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ContentControl Content="{Binding ContentView}" DataContext="{Binding DataContext, ElementName=userControl}" Grid.Row="0" Grid.ColumnSpan="2"/>
        <!--<Button Style="{DynamicResource AccentedSquareButtonStyle}" Content="בחר" Grid.Row="1" Grid.Column="1" Command="{Binding SelectCommand}"/>
        <Button Content="בטל" Grid.Row="2" Command="{Binding CancelCommand}"/>-->
    </Grid>
</UserControl>
